name: dsap-capstone
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip

  # Core data science
  - numpy
  - pandas
  - matplotlib
  - scikit-learn
  - statsmodels

  # Data sources
  - yfinance

  # Utilities
  - requests
  - beautifulsoup4

  # Google Trends (via pip)
  - pip:
      - pytrends
